import numpy as np
import pandas as pd
import seaborn as sb
import random

# basic math operations with numpy, pandas, matplotlib and seaborn.

m = np.array([[1, 3, 5], [7, 5, 4, ], [3, 4, 6]])
n = np.random.randint(1, 10, size=(2, 2))
I = np.eye(3)
print(I)